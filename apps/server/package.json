{
  "name": "@ai-mud/server",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "node --env-file=../../.env --watch --import tsx src/main.ts",
    "build": "tsc -p tsconfig.json",
    "typecheck": "pnpm --filter @ai-mud/shared build && pnpm --filter @ai-mud/content build && pnpm --filter @ai-mud/game-rules build && pnpm --filter @ai-mud/ai-prompts build && tsc -p tsconfig.json --noEmit",
    "test": "pnpm --filter @ai-mud/shared build && pnpm --filter @ai-mud/content build && pnpm --filter @ai-mud/game-rules build && pnpm --filter @ai-mud/ai-prompts build && vitest run --passWithNoTests",
    "lint": "pnpm --filter @ai-mud/shared build && pnpm --filter @ai-mud/content build && pnpm --filter @ai-mud/game-rules build && pnpm --filter @ai-mud/ai-prompts build && tsc -p tsconfig.json --noEmit",
    "db:generate": "drizzle-kit generate",
    "db:migrate": "drizzle-kit migrate",
    "db:verify-migrations": "tsx scripts/verify-migrations.ts",
    "db:verify-world-reset": "tsx scripts/verify-world-reset.ts",
    "verify:npc-simulation": "tsx scripts/verify-npc-simulation.ts",
    "e2e:real": "node --env-file=../../.env --import tsx scripts/run-real-player-e2e.ts",
    "test:integration:postgres": "pnpm --filter @ai-mud/shared build && pnpm --filter @ai-mud/content build && pnpm --filter @ai-mud/game-rules build && pnpm --filter @ai-mud/ai-prompts build && vitest run src/db/postgres-integration.test.ts src/db/game-concurrency.integration.test.ts src/db/world-runtime.integration.test.ts src/db/asset-mutation.integration.test.ts"
  },
  "dependencies": {
    "@ai-mud/content": "workspace:*",
    "@ai-mud/game-rules": "workspace:*",
    "@ai-mud/ai-prompts": "workspace:*",
    "@ai-mud/shared": "workspace:*",
    "@fastify/cookie": "^11.0.0",
    "@fastify/cors": "^11.0.0",
    "bcryptjs": "^2.4.3",
    "drizzle-orm": "^0.36.0",
    "fastify": "^5.0.0",
    "pg": "^8.13.0",
    "zod": "^3.23.0"
  },
  "devDependencies": {
    "@types/bcryptjs": "^2.4.6",
    "@types/pg": "^8.11.0",
    "drizzle-kit": "^0.27.0",
    "tsx": "^4.19.0",
    "typescript": "^5.5.0",
    "vitest": "^2.0.0"
  }
}
