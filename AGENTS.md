# AI MUD 开发入口

## 当前开发计划

下一阶段的唯一实施入口是 `docs/implementation/2026-09-18/README.md`。先读总控、`00-execution-contract.md`、`01-shared-contracts.md`，再读用户指定的一个版本包。一个版本包内同时提供产品 spec、实现步骤、测试、验收与回退要求。

此入口只指定未来开发顺序，不表示版本已经实现。当前运行时版本以 `packages/shared/src/version.ts` 为准。

## 文档优先级

系统/用户本次明确要求 > 本文件与新总控的范围约束 > 指定版本包 > 新共享合同 > 旧文档中的实现参考。

`docs/superpowers/plans/2026-07-29-v0.10.7-ai-native-npc-intent-execution-pack.md` 和同日 v1.0 交付计划保留为历史参考；它们的未来排期由新总控替代。不要并行执行旧计划和新版本包。`CLAUDE.md` 的运行命令、目录约束、当前版本描述仍然适用；其“AI may only produce text”描述的是旧运行时，新增候选决策能力必须按 v0.13.0 受控引入，不能提前放宽资产权限。

## 必守事项

- 服务端拥有权威状态；规则裁决数值与合法性；模型只能选择合法候选或生成非权威表现文本。
- 不实施用户未指定的后续版本。v1.1.0 是 DESIGN_ONLY，必须另有明确开工指令。
- 不在数据库事务内等待模型网络请求；不绕过资产服务、账本、授权或幂等检查。
- 不在生产环境运行迁移、重置、仿真或真实模型批量测试，除非用户另行明确授权。
- 每次交接记录源码 head、已通过/未运行/失败的验证，不把 mock、加速仿真、静态审查写成真实试玩或生产运行证据。
- 不批量更名旧文档、不做与当前任务无关的重构，不改已执行迁移。
- 详细开发规范与交接模板见新实施目录。
