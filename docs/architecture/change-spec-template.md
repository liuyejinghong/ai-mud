# 功能增删 / 模块扩展规格模板

版本3.0；模板不是实现完成记录。简单模块内修复可在PR填写精简版；涉及跨模块/资产/存档必须完成相关章节。

## 0. 基线与授权

- 用户授权的范围、exact HEAD、当前产品/兼容版本：
- 规格状态：DRAFT / APPROVED / IMPLEMENTED；不得跳过真实验收。
- 主所有者模块：
- 参与模块与通过的公开接口：
- 明确不做：

## 1. 玩家结果与反例

玩家将可以做什么、看见什么后果：
为什么已有内容/能力不够：
至少一个不应被允许的玩家/模型行为：
不是“添加XManager/集成某模型/增加若干字段”，而是实际行为结果。

## 2. 能力与模块地图

| 新增/修改能力 | 所有者 | 消费者 | 公开入口/输入输出 | 原依赖/新依赖 |
|---|---|---|---|---|

是否新增模块？已有模块不能容纳的理由是什么？
是否改变 module-catalog 允许边、public exports 或数据归属？列 Architecture Policy Change，未经独立审查不得直接放宽。
哪些不相关模块应保持完全不变？测试如何验证它们不依赖本功能内部？

## 3. 命令合同

名称、主体与权限、scope、commandId、worldEpoch、输入schema、返回结果与错误：
前置条件、合法候选及不允许的结果：
旧状态/并发冲突/重复请求的结果：
涉及随机时的seed/算法/内容版本：
模型建议与玩家原始意图怎样区分？是否需要明确确认？

## 4. 事务与数据所有权

| 事实/表列 | 唯一写所有者 | 使用的public写能力 | 锁根/顺序 | 本次变化 |
|---|---|---|---|---|

同一事务必须成立的内容：
可延迟/可重建内容：
资产从哪里来、到哪里去、哪些被预留/消耗：
会触及既有预留/托管吗？失败与取消如何处理？
在哪些阶段注入异常，预期所有状态如何回滚：
不得“先转账，再发事件希望任务最终完成”。不得在事务里等网络模型或玩家输入。

## 5. Query / Event / 展示

Query 的可见字段、批量读取、快照/revision语义：
事件生产者/消费者、payload版本、受众、幂等键、乱序/重复/漏通知策略：
文字与像素如何消费同一事实：
敏感字段、NPC私有记忆、未发现信息怎样排除：
不能把所有事件广播给所有玩家，也不能把projection当锁定后的交易事实。

## 6. 生命周期与存档

on/off 的含义；已有进行中行为能否继续结算：
旧内容ID/未知字段/旧存档的兼容或显式拒绝：
迁移前检查、幂等、失败恢复/备份方案：
若删除此功能：关闭入口、处理义务/资产、取消订阅、保留兼容、最终删除的顺序：
不要求在线热插拔，不允许通过删除文件/清库逃避未履行义务。

## 7. 有界性与观测

每命令/tick的工作量、SQL/锁等待预算、模型调用/超时/费用预算：
队列/缓存/日志最大范围与清理/重建：
正常/超载/模型失败时用户看到什么：
哪些数值是初始假设，哪些有实测证据：

## 8. 验收矩阵

| ID | 场景 | 层级（纯规则/合同/PG/E2E/真人/真实模型） | 精确预期 | 状态/证据 |
|---|---|---|---|---|
| AC-01 | 正常 | | | NOT_RUN |
| AC-02 | 权限/范围错误 | | | NOT_RUN |
| AC-03 | 重复/并发/部分失败 | | | NOT_RUN |
| AC-04 | 关闭/重启/旧存档 | | | NOT_RUN |
| AC-05 | 新增非法依赖被门禁拒绝 | | | NOT_RUN |

每项关联适用PC约束，未知项写BLOCKED原因；不拿mock代替PG、真人或真实模型。

## 9. 小步执行与独立审查

按依赖拆任务：合同/失败测试 → 最小实现 → 迁移调用 → 删除旧入口 → 独立验收。
列真实文件路径、每步预期、允许改动范围。不得为了赶进度一并升级模型/框架/经济参数。
交付记录 exact HEAD、实际运行命令、测试数量、未运行项、变更API和删掉的架构例外。
