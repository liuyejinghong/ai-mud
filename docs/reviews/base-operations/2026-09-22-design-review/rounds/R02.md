# R02 交互操作（尺子一 + U/T 条款）短报告

- 状态：已完成（受 B001 事故影响分两段：故障段 06:11–06:17Z 定性；健康段 07:13–07:32Z 于 r02 会话取证）
- 输入边界：r02 新档（review-r02-k4t8qw），视口 1440×900，IAB；r01 基地因事故触发源保留仅做短窗观察

## 覆盖与证据（新操作）

| 条目 | 结论 | 证据 |
|---|---|---|
| 一B1 受理反馈（健康态） | 符合：接单/交付/采购/制造/倍率全部即时受理（≤2s），状态文本就地更新 | E-R02-011~014 |
| 一B1 受理反馈（故障态） | 违规候选：注册/登录挂起 60s 仅"请稍候…"，无进度/可取消/原因；失败后 alert 仅原始状态码 | E-R02-002/003/004 |
| 一B2 结果反馈 | 大体符合：余额/库存/订单状态变化精确可指（+700、-80、6→2）；**例外**：制造工单进度无速率、无预计完成，材料未预扣、机器人仍显示"待命/空闲"，进度机制不可读 | E-R02-012/013/014 |
| 一B3 长任务 | 部分违规：采购有"运输途中·预计到货"（好），制造无预计完成（信息不一致）；项目步骤"待开工"无原因（R01-F02 待验，r02 未开工无法验证） | E-R02-013/014 |
| 一A1/A2 连续性与返回 | 符合：单页面板栈，无路由跳转；详情/地图/列表切换均保持主界面 | E-R02-008~014 |
| 一C1 关键状态就地可读 | 大体符合：决策现场可读余额/库存/价格；制造进度不可读例外 | 同上 |
| 一D1 同类操作一致 | 违规候选：订单列表操作后**重排**，同名订单可双卡并存（可接单+进行中），浮动按钮不绑定卡片，按位置点击会误选 | E-R02-012 |
| 一D2/U4 控件可操作性 | 违规候选：全站 role 定位失效（登录页签、×4、项目卡、机器人卡点击超时，force 亦超时），坐标点击正常——存在隐藏重复渲染控件；对键盘/辅助技术用户是真实障碍（R01a 的 ×4 count=2 同根） | E-R02-005 |
| U8 防错 | 部分符合：取消项目有原生 confirm 门槛；但原生对话框样式与游戏脱节 | E-R02-006 |
| T6/V05 中文与视口 | 符合：720×450（等效 200%）无截断无遮挡；密码字段 type=password 掩码正常（撤销 R01a 的明文回显疑点，属工具行为） | E-R02-004 |
| 加载 | 摩擦：新访客首开约 30s"会话恢复中"才出现登录页，无进度指示 | E-R02-001 |

## 本视角结论
健康态下核心操作的受理与结果反馈链**成立**，经济数字全部精确；主要摩擦集中在：订单列表重排/双卡辨识、制造进度不可读、控件 role 不可达（辅助技术）。故障态（B001）下的"请稍候…"无反馈与原始状态码文案为次要发现。

## 候选发现
- R02-F01（P1 候选，一D1/U5）：订单列表操作后重排+同名双卡+浮动按钮不绑定卡片 → 误操作风险。置信度：确认（复现于接单→交付全程）。
- R02-F02（P1 候选，一D2/U4/T6）：全站控件存在隐藏重复实例，role/键盘路径不可达。置信度：确认（4 类控件复现）。
- R02-F03（P2 候选，一B1/M3）：制造工单进度不可读（无速率/预计完成/因果说明，机器人状态与工单状态矛盾）。置信度：确认（观察 12 分钟 0/30）。
- R02-F04（P2 候选，一B1）：错误文案暴露原始状态码（504/804/502），无玩家语言指引。置信度：确认。
- R02-F05（P2 候选）：新访客首开 ~30s 无进度"会话恢复中"。置信度：确认（1 次，可能含服务因素）。

## 未测
夜间转换（18:00）、项目步骤开工（故意回避——B001 触发源）、采购到货入库（运输 4 小时）、制造完工（约 3 游戏小时，×4 挂机中补测）。
