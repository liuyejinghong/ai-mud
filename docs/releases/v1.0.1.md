# v1.0.1：基地试玩修复版

- 发布范围：现有 VPS 内部试玩站，保留原有玩家存档；不开放新的付费模型能力。
- 源码基线：`main@041fe3f86278c85869ef5b47e06873fdb9d8750c`；本版从隔离修复分支构建，部署时另记录精确提交与镜像身份。

## 玩家可见变化

- 基地首页集中显示目标、时间和下一步；建设位、项目状态、失败与恢复入口留在当前任务区域。经营、制造、协作作为可返回的工作区，窄屏和长账号仍能找到主动作。
- 物资显示总量、已占用、可支配量、占用它的工程或工单及在途采购；建设、批量制造、已接订单在各自操作现场说明缺口。
- 协作活动与异常可见，历史默认收起；引导说明开局已有发电和首工程增加容量；已完成/已取消制造工单不再显示取消。

## 修复的服务端路径

- 订单交付只消费可支配库存，超出时返回资源不足，不让数据库约束错误代替业务反馈。
- 协作请求按真实缺工状态创建，同步骤已有待处理或已接受请求不重复创建；跨组支援可跨 tick 出工，步骤完成后结案。

## 兼容与验证

- `PRODUCT_VERSION=1.0.1`、`apiVersion=48`。快照新增库存占用与来源字段；前后端作为同一次构建发布。数据库迁移、内容版本、规则数值和真实模型模式均未改变。
- 发布前本地：web 187/187、含隔离真 PostgreSQL 的 server 557/557，类型检查、架构检查和全仓构建通过；独立 Q 的可见路径为隔离浏览器 mock，不能代替线上真人或真实存档验收。
- 线上切换前备份数据库并保留旧镜像；发布后回读精确 Git 提交、容器、健康接口、静态资源缓存和可操作的玩家入口。若失败，先恢复旧镜像，存档只在确证需要时由备份恢复。

已知未验收：真人首访、自然等待/隔天回访、真实 Jev、实际 200% 缩放和读屏。节奏、成长和功率口径的候选调查仍未完成。
