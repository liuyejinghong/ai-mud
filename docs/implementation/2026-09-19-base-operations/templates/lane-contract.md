# 单线并发合同模板

状态：DRAFT，由对应P在实施时填写并冻结。本文不是空白授权。

## 基线与目标

- version / packageId / owner：
- actual HEAD / contractVersion / 用户授权范围：
- 玩家结果与本线输出：
- 明确非目标：
- 依赖包/真实public能力 / 可暂mock能力：

## 文件所有权（逐文件）

| path | 已有或NEW | 唯一写者 | 模块/层 | 允许改什么 | 测试文件 |
|---|---|---|---|---|---|

禁止与同时active的其他实现线有写交集。大目录只能描述参考范围，不能作为最终通配写授权；共享文件必须指定owner。未登记新文件先请求扩充清单。

## 公开合同

| 能力/方法 | 提供者 | 消费者 | 输入输出/schema | 错误 | scope/权限 | 查询或事务参与 |
|---|---|---|---|---|---|---|

相关revision/epoch/contentRef、幂等键、锁根/顺序、时间单位/精度：
共同样例/fixture/接口测试路径：
涉及存储列/事实的唯一写者与读者：

## 实施与验证

| 子步骤 | 前置 | 修改/新增文件 | 精确结果 | 验收ID/命令 |
|---|---|---|---|---|

至少包括正常、资源不足、重试/并发、授权错误、部分失败、暂停/恢复、版本变化。
各模型/服务不可用时行为：
真正集成何时发生；mock退出条件：
独立数据库/端口/配置与销毁范围：

## 接缝与变更控制

谁接线、何时提供接口、谁改schema/迁移、谁维护共享样例：
合同变更提案/独立确认记录：
最小回退/在途义务/旧存档策略：
预计交接的HEAD/证据格式：

不能以“交给主代理处理”省略数据/权限/失败含义。确切规则未冻结则标BLOCKED，不各自猜一个默认值。
