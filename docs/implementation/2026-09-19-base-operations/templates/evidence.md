# 实施与独立验收证据模板

## 1. 范围

- 版本/工作包/合同版本：
- 用户授权范围与不做事项：
- 起点HEAD、提交HEAD、集成候选HEAD、工作树状态：
- 实際改动文件、公开API、数据所有权、删除的旁路/例外：
- 当前规则/内容release/模型SDK与模型ID/场景seed：

## 2. 验证明细

| 命令/场景 | 环境与数据源 | 实际测试数量 | PASS/FAIL/NOT_RUN/BLOCKED | 结果/日志位置 |
|---|---|---:|---|---|

标明是否读取.env，必须确认测试DB不是生产；不贴密钥、cookie或完整敏感对话。
测试数0不能算通过。lint=tsc不能冒充两个独立质量证据。历史CI与当前提交不同要注明。

## 3. 门槛

| 本版GATE/适用PC | 正常/失败/并发/旧版本场景 | 证据类型 | 精确预期与实际 | 状态 |
|---|---|---|---|---|

证据类型明确分为静态走读、pure/unit、contract/mock、真PG、E2E、真人、真实模型、负载/恢复。mock UI不等于真实后端行为；源码阅读不等于测试运行。

## 4. 真实连接与限制

- 哪些调用已连接运行系统，哪些仍用替身：
- 是否存在重复状态、读时副作用、未注册写入口：
- 模型调用预算/授权/实际用量与费用可得范围：
- 未运行验证、已知风险、需下一线处理的问题：

## 5. 迁移/运行/恢复

- 实际schema journal和新增迁移，不改旧迁移：
- 数据前后核对、暂停/排空在途步骤：
- 内容/协议/存档兼容策略：
- 模块关闭后的在途工单/托管/预留如何处理：
- 备份与恢复是否在隔离环境实跑：
- 回退操作风险与需要单独授权的动作：

## 6. 独立结论

ENGINEERING：
PLAYTEST：
LIVE_MODEL_GATE（按purpose）：
RELEASE_READY：

每项可不同。没有真人写PLAYTEST_PENDING；没有真实调用写LIVE_MODEL_NOT_RUN；适配器绿不代表模型合理性通过。Q签收的HEAD与I发布候选必须一致，改动后重跑相关门槛。

报告不授权自动合并、生产发布、reset或下一版本开工。
