# 2026-09-25 修复批次实施结果

状态：**LOCAL_ENGINEERING_VERIFIED / NOT_MERGED / NOT_DEPLOYED**。本轮用户明确授权 U、C、T、M；E 节奏/成长调查及规则调参不在本批。代码基线为本地 `main` 的 `041fe3f86278c85869ef5b47e06873fdb9d8750c`，受测集成代码提交为 `65442ed01a6af84479cd81b772481614e8b6eeb2`，分支 `codex/review-remediation-20260925`。原评审分支与未跟踪交接资料保留原状。远端 `main` 因本机代理连不上 GitHub 未读回，不能声称它仍等于本地基线；线上部署 SHA 也未读回。

## 已实施

| 问题 | 代码变化与玩家结果 | 本批证据 |
|---|---|---|
| F01/F02/F05 · U | 现有基地、经营、制造、协作分为可返回工作区；目标、地图、所选对象、动作和结果相接，窄屏详情可返回地图；长邮箱与多设备不挤走计时。暂停项目就地说明并恢复，受阻时不承诺恢复即施工；页首时钟失败在时间区提示。采购/制造草稿和对象选择跨工作区/轮询保留。 | web 单测、[Q 首屏/路径/键盘 mock](evidence/Q-local-mock-acceptance.md)；当前视口 1200×800/701、720×450 与 600×400 CSS 空间近似 |
| F03/C07 · C | 协作活动常显、53 条历史默认折叠且逐 requestId 可查，主叙事用设备名。后端按真正缺工步骤检测，已 pending/accepted 不重复建单；接受的跨组 helper 可跨 tick 出工、复电后返回原步骤，步骤完成即结案，机器人读与结算处于同一事务。旧历史不删。 | Q 历史 mock；C07 单测、真 PG 事务可见性与 B001 同事务审计回归；**旧线上 53 条每条成因未读回** |
| F04/C08 · M | 快照从 canonical 库存透出总量、占用及同基地工程/工单来源；建设、制造批量、已接订单与物资详情显示可支配量、缺口、来源和在途。失败后刷新现场。订单交付仅在 `quantity-reservedQuantity >= 需求` 时原子扣减，超可用量走业务资源不足，不再落 PG 约束错误。 | [Q-M 初版 FAIL→同条件复验 PASS（mock）](evidence/Q-local-mock-acceptance.md#a06-同条件复验)；本机开发 PG 随机临时库总 8/占 8 与总 8/占 2 定向 5/5 |
| F06/F07 · T | 引导按开局 15 kW、首工程增加 5 kW、接单/采购/制造已可用来写；完成/取消工单保留结果而不再给取消入口，服务端终态拒绝仍在。 | 单测和 Q 终态/引导 mock；真实新档完整首工程前后行为未由本批浏览器接线重测 |

`BaseResourceDto` 兼容性增加必填 `reservedQuantity`、`reservationSources`；其余 U/C/T 使用已有快照与命令，未增路由、前端计时权威、依赖、数据库迁移、架构 allowlist 或产品版本。新前端需要新快照字段，部署时前后端须使用同一构建；本批未发布。

## 实际验证

- `pnpm --filter @ai-mud/web test`：33 文件、187/187；`pnpm --filter @ai-mud/web typecheck`：通过。
- 显式指向已核对的本机开发 PostgreSQL 容器（127.0.0.1:55432）的 `DATABASE_URL`，执行 `pnpm --filter @ai-mud/server test`：63 文件、557/557，10 个需要数据库的测试文件均使用随机临时库；其中包含 C07、B001/B002、M 订单条件扣减、建设/制造/资产回滚。
- `pnpm --filter @ai-mud/server typecheck`、`pnpm arch:check`（未覆盖新增违规 0）、`pnpm build`：通过。
- 独立 Q 用本机 Chrome、隔离 mock 快照和当前视口截图验证 U/C/T；Q-M 在 `fea6834` 发现 A06 现场来源缺失并保留 FAIL 证据，在 `fe81bf0` 以同起点只复核该缺口为 PASS。上述浏览器证据不属于真 PG 或真人证据。

M 只读调查经 Agent Bridge → ZCode 执行；原生模型记录确认调查请求使用 GLM-5.3-Flash。后续 M 实现请求实际回落到 GLM-5.3（尽管先前 `/model` 返回 Flash），因此不把该实现记为 Flash 交付；最终结论以代码 diff、测试与集成复核为准。后续未继续派发外部任务。

## 尚未验收与回退

A01–A05/A07/A09 的本地可见路径已通过隔离 mock；A06 有 mock 复验及订单真 PG 边界。真实浏览器连真实服务的 A08/A10 全链、真人首访、原档长期回访、实际浏览器 200% 缩放和读屏、真实 Jev/尘暴/多用户容量、自然事件、线上 SHA 与部署回读均为 **NOT_RUN**。C01/C02/C03/C04/C05/C06/C09/C10 继续保持调查候选；C07 的代码缺陷已修于本地，但旧线上请求不作追溯性改写。一个包的本地通过不等于 v1.0 游戏或生产通过。

U、C、T、M 分别有独立提交，可沿反向依赖回退呈现和逻辑；M 前端与新增快照字段须成对回退，先撤前端消费者再撤后端 DTO。C07 无迁移且不清历史，回退其后端提交即可恢复原调度代码。任何真实部署/回滚和存档操作仍需另行授权。
