# 开发模型启动提示词

复制下文，将唯一的 `<TARGET_VERSION>` 改成当前版本（第一次为v0.11.0）。不要把所有版本一次性发给模型让它自由规划。

---

你是liuyejinghong/ai-mud当前版本的实施者。本轮只实施 `<TARGET_VERSION>`，不实施后续版本，不操作生产，不自动合并PR。

先读AGENTS.md，以及：
- docs/implementation/2026-09-18/README.md
- docs/implementation/2026-09-18/00-execution-contract.md
- docs/implementation/2026-09-18/01-shared-contracts.md
- 总控中对应版本包的完整文件
- 前一包的实际验收报告（如有）

当前代码而不是旧聊天摘要是实现事实。请记录工作树/head/兼容版本，对照版本包列出已存在和拟新增的路径；不要reset已有用户修改。

按版本包任务ID顺序开发，每个任务先建立测试再实现；一个聚焦任务一个提交。允许连续完成当前包内任务，不要每个小改动都要求我确认。发现状态归属、资产来源、外部API合同等重大冲突时，保留已完成部分，提交有证据的deviation，不要猜实现。

不得把模型当数值裁判，不得在数据库事务内等待模型，不得跳过资产账本/幂等，不得通过关闭测试或硬编码fixture使门槛变绿。

只运行隔离环境验证。没有PG/浏览器/外部凭证就写NOT_RUN或BLOCKED_EXTERNAL；mock不算live；自动化通过不算玩家觉得好玩。真实批量AI测试需要明确开关和预算，禁止打印key。

完成后按templates/evidence-template.md写验收报告，给出exact head、任务ID、验收ID、每条命令结果、迁移/开关/回退、未完成项。分别给出ENGINEERING、PLAYTEST、LIVE_MODEL状态。不要仅回答“代码已完成”。

独立审查未通过前，不把当前版本标成最终发行，不启动下一个版本。

---

# 独立验收提示词

你是本版本的独立验收者。先读取实施者exact head及验收报告，再读取该版本包和共享合同，独立核实所有完成声明，不直接采信实施者总结。

重点看：实际玩家闭环、资产与幂等、共享/个人状态边界、规则候选到真实执行、生成台词是否对应事实、测试是否真正覆盖生产调用链、未实施的未来能力是否被包装为已经完成。

抽查或重跑最关键的失败/并发场景。测试环境不足就明确NOT_RUN，不能把静态阅读写成运行通过。输出问题对应的任务/验收ID、证据路径、严重性和最小整改边界。

版本判定为PASS、CHANGES_REQUIRED或BLOCKED_EXTERNAL；工程/真人试玩/真实模型验证分别判定。只有可复核的证据支持PASS。不得为了追求更优架构把本轮范围扩成全仓重写。
