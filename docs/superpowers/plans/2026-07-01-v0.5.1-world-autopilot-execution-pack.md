# v0.5.1 世界自动运转执行包

> **给执行代理：** 必须使用 `superpowers:subagent-driven-development`（推荐）或 `superpowers:executing-plans` 按任务逐项执行。步骤使用 checkbox (`- [ ]`) 跟踪进度。

**目标：** 让 v0.5.0 的 Living NPC 从“管理员手动推进可运行”升级为“服务端按现实时间自动推进，并且玩家/管理员上线时可安全补结算”的世界运行内核。

**架构：** 新增世界运行时状态表和 `WorldRuntimeService`，用服务器时间、结算游标和轻量租约控制 NPC 世界推进，避免重复结算和多实例重复 tick。先把现有 NPC 仿真改成只读预演，避免后台“仿真”按钮偷偷修改真实世界；再把真实世界推进集中到一个可审计入口，后台按钮、定时器、玩家上线补结算都走同一入口。

**技术栈：** TypeScript monorepo, Fastify, Drizzle ORM, PostgreSQL migration SQL, React/Vite, `@ai-mud/game-rules`, `@ai-mud/content`, Vitest, Playwright.

---

## 0. 执行前逻辑审计

当前 v0.5.0 状态：

- 已有真实 NPC actor、NPC 库存、NPC 行动、共享资源、市政金库、NPC 市场交易和后台 NPC 面板。
- `NpcService.settleNpcWorld(now)` 已经可以推进一轮 NPC 行为。
- 后台 `POST /admin/npcs/settle` 可以手动推进真实世界。
- 后台 `POST /admin/npcs/simulate` 当前调用 `NpcService.runNpcSimulation(days, startAt)`。

必须先修的逻辑问题：

- `runNpcSimulation()` 当前会循环调用真实 `settleNpcWorld()`。如果它运行在真实数据库仓储上，就会改变真实世界状态。这不符合“仿真报告”的语义。
- 因此 v0.5.1 的第一个任务必须是：把仿真改为只读预演，或者明确改名为破坏性推进。按产品预期，应选择只读预演。

本版本必须坚持的边界：

- 自动推进不能绕过 `NpcService`。
- 自动推进不能让 AI 获得资产裁决权。
- 自动推进不能用“每天给 NPC 发资源/钱”的假模型代替真实行动。
- 离线补结算必须有步数上限，避免玩家隔很久上线时请求卡死。
- 多次请求同时触发补结算时，不能重复推进同一时间段。

本版本不做：

- 增加更多 NPC。
- 引入 AI 对话、AI 任务、AI 记忆。
- 做复杂天气、资源刷新、工资调参。
- 统一玩家个人地图资源和 NPC 共享资源。
- 做多服分布式强一致世界模拟；首版只做单库轻量租约，足够支撑内测。

## 1. 文件范围

- Create: `apps/server/drizzle/0007_world_runtime_state.sql`
- Modify: `apps/server/src/db/schema.ts`
- Modify: `apps/server/src/db/schema.test.ts`
- Create: `apps/server/src/modules/world-runtime/world-runtime.repository.ts`
- Create: `apps/server/src/modules/world-runtime/world-runtime.service.ts`
- Create: `apps/server/src/modules/world-runtime/world-runtime.service.test.ts`
- Create: `apps/server/src/modules/npc/npc.simulation-repository.ts`
- Modify: `apps/server/src/modules/npc/npc.service.ts`
- Modify: `apps/server/src/modules/npc/npc.service.test.ts`
- Modify: `apps/server/src/modules/admin/admin.routes.ts`
- Modify: `apps/server/src/modules/admin/admin.routes.test.ts`
- Modify: `apps/server/src/modules/game/game.routes.ts`
- Modify: `apps/server/src/modules/game/game.routes.test.ts`
- Modify: `apps/server/src/config/env.ts`
- Modify: `apps/server/src/app.ts`
- Modify: `apps/server/src/app.test.ts`
- Modify: `apps/web/src/features/admin/adminApi.ts`
- Modify: `apps/web/src/features/admin/NpcAdmin.tsx`
- Modify: `apps/web/src/features/admin/NpcAdmin.test.tsx`
- Modify: `apps/web/e2e/auth-smoke.spec.ts`
- Create: `docs/releases/v0.5.1.md`

## 2. 任务 1：NPC 仿真改为只读

目标：后台仿真报告不能修改真实世界。

### 文件

- Create: `apps/server/src/modules/npc/npc.simulation-repository.ts`
- Modify: `apps/server/src/modules/npc/npc.service.ts`
- Modify: `apps/server/src/modules/npc/npc.service.test.ts`
- Modify: `apps/server/src/modules/admin/admin.routes.test.ts`

### 步骤

- [ ] **步骤 1：添加 RED 测试，证明仿真不会修改真实 NPC 世界**

  Add this test to `apps/server/src/modules/npc/npc.service.test.ts`:

  ```ts
  it("runs simulation reports without mutating the live NPC world", async () => {
    const repo = new InMemoryNpcRepository();
    const service = new NpcService(repo);
    const startAt = new Date("2026-07-01T00:00:00.000Z");

    await service.ensureWorldSeeded(startAt);
    repo.seedMarketItem({
      itemId: "wild_berry",
      quantity: 5,
      targetQuantity: 20,
      baseBuyPriceCopper: 3,
      baseSellPriceCopper: 5
    });
    const before = {
      actors: structuredClone(repo.actors),
      actions: structuredClone(repo.actions),
      resources: structuredClone(repo.resources),
      items: structuredClone([...repo.items.entries()]),
      treasury: structuredClone(repo.treasury),
      transactions: structuredClone(repo.transactions)
    };

    const report = await service.runNpcSimulation(1, startAt);

    expect(report.days).toBe(1);
    expect(report.actionCount).toBeGreaterThan(0);
    expect(repo.actors).toEqual(before.actors);
    expect(repo.actions).toEqual(before.actions);
    expect(repo.resources).toEqual(before.resources);
    expect([...repo.items.entries()]).toEqual(before.items);
    expect(repo.treasury).toEqual(before.treasury);
    expect(repo.transactions).toEqual(before.transactions);
  });
  ```

- [ ] **步骤 2：运行 RED**

  ```bash
  CI=true pnpm --filter @ai-mud/server test -- npc.service.test.ts
  ```

  预期：FAIL，因为 `runNpcSimulation()` 会修改 `repo.actions`、resources、inventory、treasury 或 transactions。

- [ ] **步骤 3：实现克隆版仿真仓储**

  Create `apps/server/src/modules/npc/npc.simulation-repository.ts`:

  ```ts
  import type {
    NpcActionRecord,
    NpcActorRecord,
    NpcEventRecord,
    NpcInventoryRecord,
    NpcMarketInventoryRecord,
    NpcRepositoryPort
  } from "./npc.service.js";
  import { CORRUPT_FOREST, type NpcDefinition } from "@ai-mud/content";
  import type { GameLocationId, GridPositionDto, ItemId } from "@ai-mud/shared";

  function cloneDate(date: Date) {
    return new Date(date.getTime());
  }

  function cloneAction(action: NpcActionRecord): NpcActionRecord {
    return {
      ...action,
      startedAt: cloneDate(action.startedAt),
      endsAt: cloneDate(action.endsAt),
      payload: structuredClone(action.payload)
    };
  }

  export class NpcSimulationRepository implements NpcRepositoryPort {
    private actors: NpcActorRecord[] = [];
    private resources: Awaited<ReturnType<NpcRepositoryPort["listWorldResourceNodes"]>> = [];
    private treasury: Awaited<ReturnType<NpcRepositoryPort["findMunicipalTreasury"]>> = null;
    private inventory = new Map<string, NpcInventoryRecord[]>();
    private actions: NpcActionRecord[] = [];
    private events = new Map<string, NpcEventRecord[]>();
    private marketInventory: NpcMarketInventoryRecord[] = [];
    private marketTransactionCount = 0;

    static async fromLive(repo: NpcRepositoryPort) {
      const simulation = new NpcSimulationRepository();
      simulation.actors = (await repo.listNpcActors()).map((actor) => ({
        ...actor,
        position: actor.position ? { ...actor.position } : null,
        lastHungerSettledAt: cloneDate(actor.lastHungerSettledAt)
      }));
      simulation.resources = (await repo.listWorldResourceNodes()).map((resource) => ({
        ...resource,
        position: { ...resource.position }
      }));
      const treasury = await repo.findMunicipalTreasury("blackpine_outpost");
      simulation.treasury = treasury ? { ...treasury } : null;
      simulation.actions = (await repo.listNpcActions()).map(cloneAction);
      simulation.marketInventory = (await repo.listMarketInventory("blackpine_outpost")).map(
        (item) => ({ ...item })
      );
      simulation.marketTransactionCount = await repo.countNpcMarketTransactions();

      for (const actor of simulation.actors) {
        simulation.inventory.set(
          actor.id,
          (await repo.listNpcInventory(actor.id)).map((item) => ({ ...item }))
        );
        simulation.events.set(
          actor.id,
          (await repo.listNpcEvents(actor.id, 20)).map((event) => ({
            ...event,
            createdAt: cloneDate(event.createdAt)
          }))
        );
      }

      return simulation;
    }

    async listNpcActors() {
      return this.actors;
    }

    async createNpcActor(npc: NpcDefinition, now: Date) {
      const actor: NpcActorRecord = {
        id: `simulation-actor-${npc.key}`,
        actorType: "npc",
        npcKey: npc.key,
        name: npc.name,
        profession: npc.profession,
        currentLocation: npc.homeLocation,
        position: npc.homePosition ? { ...npc.homePosition } : null,
        copperBalance: npc.startingCopper,
        hunger: 5,
        lastHungerSettledAt: cloneDate(now),
        status: "active"
      };
      this.actors.push(actor);
      return actor;
    }

    async listWorldResourceNodes() {
      return this.resources;
    }

    async createWorldResourceNode(input: {
      zoneId: typeof CORRUPT_FOREST.id;
      resourceId: string;
      position: GridPositionDto;
      charges: number;
    }) {
      this.resources.push({
        zoneId: input.zoneId,
        resourceId: input.resourceId,
        position: { ...input.position },
        charges: input.charges
      });
    }

    async findMunicipalTreasury() {
      return this.treasury;
    }

    async createMunicipalTreasury(input: { settlementId: "blackpine_outpost"; copperBalance: number }) {
      this.treasury = { ...input };
    }

    async listNpcInventory(actorId: string) {
      return this.inventory.get(actorId) ?? [];
    }

    async setNpcInventoryItem(input: { actorId: string; itemId: ItemId | string; quantity: number }) {
      const rows = [...(this.inventory.get(input.actorId) ?? [])];
      const index = rows.findIndex((item) => item.itemId === input.itemId);
      if (index >= 0) rows[index] = { itemId: input.itemId, quantity: input.quantity };
      else rows.push({ itemId: input.itemId, quantity: input.quantity });
      this.inventory.set(input.actorId, rows);
    }

    async updateNpcActor(input: {
      actorId: string;
      currentLocation?: GameLocationId;
      position?: GridPositionDto | null;
      copperBalance?: number;
      hunger?: number;
      lastHungerSettledAt?: Date;
    }) {
      const actor = this.actors.find((entry) => entry.id === input.actorId);
      if (!actor) throw new Error("NPC actor not found");
      if (input.currentLocation !== undefined) actor.currentLocation = input.currentLocation;
      if ("position" in input) actor.position = input.position ?? null;
      if (input.copperBalance !== undefined) actor.copperBalance = input.copperBalance;
      if (input.hunger !== undefined) actor.hunger = input.hunger;
      if (input.lastHungerSettledAt) actor.lastHungerSettledAt = cloneDate(input.lastHungerSettledAt);
    }

    async findActiveNpcAction(actorId: string) {
      return this.actions.find((action) => action.actorId === actorId && action.status === "active") ?? null;
    }

    async listNpcActions() {
      return this.actions;
    }

    async createNpcAction(input: {
      actorId: string;
      actionType: string;
      startedAt: Date;
      endsAt: Date;
      payload: Record<string, unknown>;
    }) {
      const action: NpcActionRecord = {
        id: `simulation-action-${this.actions.length + 1}`,
        status: "active",
        ...input,
        startedAt: cloneDate(input.startedAt),
        endsAt: cloneDate(input.endsAt),
        payload: structuredClone(input.payload)
      };
      this.actions.push(action);
      return action;
    }

    async markNpcActionCompleted(actionId: string) {
      const action = this.actions.find((entry) => entry.id === actionId);
      if (!action) throw new Error("NPC action not found");
      action.status = "completed";
    }

    async listNpcEvents(actorId: string, limit: number) {
      return (this.events.get(actorId) ?? []).slice(0, limit);
    }

    async updateWorldResourceNodeCharges(input: { resourceId: string; charges: number }) {
      const resource = this.resources.find((entry) => entry.resourceId === input.resourceId);
      if (!resource) throw new Error("World resource node not found");
      resource.charges = input.charges;
    }

    async updateMunicipalTreasury(input: { settlementId: "blackpine_outpost"; copperBalance: number }) {
      this.treasury = { ...input };
    }

    async listMarketInventory() {
      return this.marketInventory;
    }

    async setMarketInventoryQuantity(input: { marketInventoryId: string; quantity: number }) {
      const item = this.marketInventory.find((entry) => entry.id === input.marketInventoryId);
      if (!item) throw new Error("Market item not found");
      item.quantity = input.quantity;
    }

    async createNpcMarketTransaction() {
      this.marketTransactionCount += 1;
    }

    async countNpcMarketTransactions() {
      return this.marketTransactionCount;
    }
  }
  ```

- [ ] **步骤 4：让 `NpcService.runNpcSimulation()` 默认 dry-run**

  In `apps/server/src/modules/npc/npc.service.ts`, import the simulation repository and split the current mutation loop:

  ```ts
  import { NpcSimulationRepository } from "./npc.simulation-repository.js";
  ```

  Change:

  ```ts
  async runNpcSimulation(days: number, startAt: Date): Promise<NpcSimulationReportDto> {
    const simulationRepo = await NpcSimulationRepository.fromLive(this.repo);
    const simulationService = new NpcService(simulationRepo);
    return simulationService.runNpcSimulationInPlace(days, startAt);
  }

  async runNpcSimulationInPlace(days: number, startAt: Date): Promise<NpcSimulationReportDto> {
    // move the existing simulation loop body here unchanged
  }
  ```

  `runNpcSimulationInPlace()` must only be used by simulation repositories or tests. Real admin routes must keep calling `runNpcSimulation()`.

- [ ] **步骤 5：运行 GREEN**

  ```bash
  CI=true pnpm --filter @ai-mud/server test -- npc.service.test.ts admin.routes.test.ts
  CI=true pnpm --filter @ai-mud/server typecheck
  ```

- [ ] **步骤 6：提交**

  ```bash
  git add apps/server/src/modules/npc/npc.service.ts apps/server/src/modules/npc/npc.service.test.ts apps/server/src/modules/npc/npc.simulation-repository.ts
  git commit -m "Make NPC simulation reports read-only"
  ```

## 3. 任务 2：新增世界运行时状态

目标：用数据库记录世界自动推进游标和轻量租约，避免重复推进。

### 文件

- Create: `apps/server/drizzle/0007_world_runtime_state.sql`
- Modify: `apps/server/src/db/schema.ts`
- Modify: `apps/server/src/db/schema.test.ts`
- Create: `apps/server/src/modules/world-runtime/world-runtime.repository.ts`
- Create: `apps/server/src/modules/world-runtime/world-runtime.service.test.ts`

### 步骤

- [ ] **步骤 1：添加 RED schema 测试**

  In `apps/server/src/db/schema.test.ts`, import `worldRuntimeState` and add:

  ```ts
  it("defines world runtime state for automatic settlement", () => {
    expect(getDrizzleTableName(worldRuntimeState)).toBe("world_runtime_state");
    expect(worldRuntimeState.key.getSQLType()).toBe("text");
    expect(worldRuntimeState.lastSettledAt.getSQLType()).toBe("timestamp with time zone");
    expect(worldRuntimeState.leaseOwner.getSQLType()).toBe("text");
    expect(worldRuntimeState.leaseUntil.getSQLType()).toBe("timestamp with time zone");
  });
  ```

- [ ] **步骤 2：运行 RED**

  ```bash
  CI=true pnpm --filter @ai-mud/server test -- schema.test.ts
  ```

  预期：FAIL，因为 `worldRuntimeState` 还不存在。

- [ ] **步骤 3：实现 schema**

  Add to `apps/server/src/db/schema.ts`:

  ```ts
  export const worldRuntimeState = pgTable(
    "world_runtime_state",
    {
      key: text("key").primaryKey(),
      lastSettledAt: timestamp("last_settled_at", { withTimezone: true }),
      leaseOwner: text("lease_owner"),
      leaseUntil: timestamp("lease_until", { withTimezone: true }),
      updatedAt: timestamp("updated_at", { withTimezone: true }).notNull().defaultNow()
    },
    (table) => ({
      leaseUntilIdx: index("world_runtime_state_lease_until_idx").on(table.leaseUntil)
    })
  );
  ```

- [ ] **步骤 4：添加 migration**

  Create `apps/server/drizzle/0007_world_runtime_state.sql`:

  ```sql
  CREATE TABLE IF NOT EXISTS "world_runtime_state" (
    "key" text PRIMARY KEY NOT NULL,
    "last_settled_at" timestamp with time zone,
    "lease_owner" text,
    "lease_until" timestamp with time zone,
    "updated_at" timestamp with time zone DEFAULT now() NOT NULL
  );

  CREATE INDEX IF NOT EXISTS "world_runtime_state_lease_until_idx"
    ON "world_runtime_state" ("lease_until");
  ```

- [ ] **步骤 5：创建仓储契约**

  Create `apps/server/src/modules/world-runtime/world-runtime.repository.ts`:

  ```ts
  import { eq } from "drizzle-orm";
  import type { Db } from "../../db/client.js";
  import { worldRuntimeState } from "../../db/schema.js";

  export interface WorldRuntimeRecord {
    key: string;
    lastSettledAt: Date | null;
    leaseOwner: string | null;
    leaseUntil: Date | null;
  }

  type WorldRuntimeDb = Pick<Db, "insert" | "select" | "update">;

  export class WorldRuntimeRepository {
    constructor(private readonly db: WorldRuntimeDb) {}

    async find(key: string): Promise<WorldRuntimeRecord | null> {
      const [row] = await this.db
        .select()
        .from(worldRuntimeState)
        .where(eq(worldRuntimeState.key, key))
        .limit(1);

      return row
        ? {
            key: row.key,
            lastSettledAt: row.lastSettledAt,
            leaseOwner: row.leaseOwner,
            leaseUntil: row.leaseUntil
          }
        : null;
    }

    async upsert(input: WorldRuntimeRecord): Promise<void> {
      const existing = await this.find(input.key);
      if (existing) {
        await this.db
          .update(worldRuntimeState)
          .set({
            lastSettledAt: input.lastSettledAt,
            leaseOwner: input.leaseOwner,
            leaseUntil: input.leaseUntil,
            updatedAt: new Date()
          })
          .where(eq(worldRuntimeState.key, input.key));
        return;
      }

      await this.db.insert(worldRuntimeState).values({
        key: input.key,
        lastSettledAt: input.lastSettledAt,
        leaseOwner: input.leaseOwner,
        leaseUntil: input.leaseUntil
      });
    }
  }
  ```

- [ ] **步骤 6：运行 GREEN**

  ```bash
  CI=true pnpm --filter @ai-mud/server test -- schema.test.ts
  CI=true pnpm --filter @ai-mud/server typecheck
  ```

- [ ] **步骤 7：提交**

  ```bash
  git add apps/server/drizzle/0007_world_runtime_state.sql apps/server/src/db/schema.ts apps/server/src/db/schema.test.ts apps/server/src/modules/world-runtime/world-runtime.repository.ts
  git commit -m "Add world runtime settlement state"
  ```

## 4. 任务 3：实现有上限的自动补结算

目标：让世界按现实时间补结算，但每次请求最多推进有限步数，避免长时间离线后卡死。

### 文件

- Create: `apps/server/src/modules/world-runtime/world-runtime.service.ts`
- Modify: `apps/server/src/modules/world-runtime/world-runtime.service.test.ts`

### 规则

- Tick unit: 60 seconds.
- Lease duration: 55 seconds.
- Default max steps per call: 60.
- If no `lastSettledAt`, initialize to `floorToMinute(now)` and do not backfill历史。
- If `lastSettledAt` is behind, advance one minute at a time and call `NpcService.settleNpcWorld(tickAt)` for each step.
- If another owner lease is active, do nothing.
- After each successful step, update `lastSettledAt` so partial progress is durable.

### 步骤

- [ ] **步骤 1：添加 RED service 测试**

  Create `apps/server/src/modules/world-runtime/world-runtime.service.test.ts`:

  ```ts
  import { describe, expect, it } from "vitest";
  import { WorldRuntimeService } from "./world-runtime.service.js";
  import type { WorldRuntimeRecord, WorldRuntimeRepository } from "./world-runtime.repository.js";

  class InMemoryWorldRuntimeRepository implements Pick<WorldRuntimeRepository, "find" | "upsert"> {
    record: WorldRuntimeRecord | null = null;

    async find() {
      return this.record;
    }

    async upsert(input: WorldRuntimeRecord) {
      this.record = {
        ...input,
        lastSettledAt: input.lastSettledAt ? new Date(input.lastSettledAt) : null,
        leaseUntil: input.leaseUntil ? new Date(input.leaseUntil) : null
      };
    }
  }

  describe("WorldRuntimeService", () => {
    it("initializes runtime state without backfilling old history", async () => {
      const repo = new InMemoryWorldRuntimeRepository();
      const settledAt: string[] = [];
      const service = new WorldRuntimeService({
        repo,
        ownerId: "test-owner",
        settleNpcWorld: async (now) => {
          settledAt.push(now.toISOString());
        }
      });

      const result = await service.settleDue(new Date("2026-07-01T12:34:56.000Z"));

      expect(result).toEqual({ settledSteps: 0, skipped: false });
      expect(repo.record?.lastSettledAt?.toISOString()).toBe("2026-07-01T12:34:00.000Z");
      expect(settledAt).toEqual([]);
    });

    it("settles one minute at a time up to the max step limit", async () => {
      const repo = new InMemoryWorldRuntimeRepository();
      repo.record = {
        key: "npc_world",
        lastSettledAt: new Date("2026-07-01T12:00:00.000Z"),
        leaseOwner: null,
        leaseUntil: null
      };
      const settledAt: string[] = [];
      const service = new WorldRuntimeService({
        repo,
        ownerId: "test-owner",
        maxStepsPerRun: 3,
        settleNpcWorld: async (now) => {
          settledAt.push(now.toISOString());
        }
      });

      const result = await service.settleDue(new Date("2026-07-01T12:10:00.000Z"));

      expect(result).toEqual({ settledSteps: 3, skipped: false });
      expect(settledAt).toEqual([
        "2026-07-01T12:01:00.000Z",
        "2026-07-01T12:02:00.000Z",
        "2026-07-01T12:03:00.000Z"
      ]);
      expect(repo.record?.lastSettledAt?.toISOString()).toBe("2026-07-01T12:03:00.000Z");
    });

    it("skips settlement while another owner lease is active", async () => {
      const repo = new InMemoryWorldRuntimeRepository();
      repo.record = {
        key: "npc_world",
        lastSettledAt: new Date("2026-07-01T12:00:00.000Z"),
        leaseOwner: "other-owner",
        leaseUntil: new Date("2026-07-01T12:01:00.000Z")
      };
      const service = new WorldRuntimeService({
        repo,
        ownerId: "test-owner",
        settleNpcWorld: async () => {
          throw new Error("should not settle");
        }
      });

      await expect(service.settleDue(new Date("2026-07-01T12:00:30.000Z"))).resolves.toEqual({
        settledSteps: 0,
        skipped: true
      });
    });
  });
  ```

- [ ] **步骤 2：运行 RED**

  ```bash
  CI=true pnpm --filter @ai-mud/server test -- world-runtime.service.test.ts
  ```

- [ ] **步骤 3：实现 service**

  Create `apps/server/src/modules/world-runtime/world-runtime.service.ts`:

  ```ts
  import type { WorldRuntimeRepository } from "./world-runtime.repository.js";

  const NPC_WORLD_KEY = "npc_world";
  const DEFAULT_TICK_MS = 60_000;
  const DEFAULT_LEASE_MS = 55_000;

  function floorToMinute(date: Date) {
    return new Date(Math.floor(date.getTime() / DEFAULT_TICK_MS) * DEFAULT_TICK_MS);
  }

  export interface WorldRuntimeServiceInput {
    repo: Pick<WorldRuntimeRepository, "find" | "upsert">;
    ownerId: string;
    maxStepsPerRun?: number;
    settleNpcWorld(now: Date): Promise<void>;
  }

  export class WorldRuntimeService {
    constructor(private readonly input: WorldRuntimeServiceInput) {}

    async settleDue(now: Date) {
      const currentMinute = floorToMinute(now);
      const existing = await this.input.repo.find(NPC_WORLD_KEY);

      if (!existing) {
        await this.input.repo.upsert({
          key: NPC_WORLD_KEY,
          lastSettledAt: currentMinute,
          leaseOwner: null,
          leaseUntil: null
        });
        return { settledSteps: 0, skipped: false };
      }

      if (
        existing.leaseUntil &&
        existing.leaseUntil.getTime() > now.getTime() &&
        existing.leaseOwner !== this.input.ownerId
      ) {
        return { settledSteps: 0, skipped: true };
      }

      const maxSteps = Math.max(1, Math.floor(this.input.maxStepsPerRun ?? 60));
      let cursor = existing.lastSettledAt ?? currentMinute;
      let settledSteps = 0;

      await this.input.repo.upsert({
        ...existing,
        leaseOwner: this.input.ownerId,
        leaseUntil: new Date(now.getTime() + DEFAULT_LEASE_MS)
      });

      while (cursor.getTime() + DEFAULT_TICK_MS <= currentMinute.getTime() && settledSteps < maxSteps) {
        cursor = new Date(cursor.getTime() + DEFAULT_TICK_MS);
        await this.input.settleNpcWorld(cursor);
        settledSteps += 1;
        await this.input.repo.upsert({
          key: NPC_WORLD_KEY,
          lastSettledAt: cursor,
          leaseOwner: this.input.ownerId,
          leaseUntil: new Date(now.getTime() + DEFAULT_LEASE_MS)
        });
      }

      await this.input.repo.upsert({
        key: NPC_WORLD_KEY,
        lastSettledAt: cursor,
        leaseOwner: null,
        leaseUntil: null
      });

      return { settledSteps, skipped: false };
    }
  }
  ```

- [ ] **步骤 4：运行 GREEN**

  ```bash
  CI=true pnpm --filter @ai-mud/server test -- world-runtime.service.test.ts
  CI=true pnpm --filter @ai-mud/server typecheck
  ```

- [ ] **步骤 5：提交**

  ```bash
  git add apps/server/src/modules/world-runtime/world-runtime.service.ts apps/server/src/modules/world-runtime/world-runtime.service.test.ts
  git commit -m "Add bounded world runtime settlement"
  ```

## 5. 任务 4：接入服务端请求和后台 Tick

目标：世界会自己走；玩家或管理员上线时也会补结算。

### 文件

- Modify: `apps/server/src/config/env.ts`
- Modify: `apps/server/src/app.ts`
- Modify: `apps/server/src/app.test.ts`
- Modify: `apps/server/src/modules/game/game.routes.ts`
- Modify: `apps/server/src/modules/game/game.routes.test.ts`
- Modify: `apps/server/src/modules/admin/admin.routes.ts`
- Modify: `apps/server/src/modules/admin/admin.routes.test.ts`

### 步骤

- [ ] **步骤 1：添加 env 开关**

  In `apps/server/src/config/env.ts`, add:

  ```ts
  WORLD_TICK_ENABLED: z
    .string()
    .optional()
    .transform((value) => value === undefined ? true : value === "true"),
  WORLD_TICK_INTERVAL_MS: z.coerce.number().int().positive().default(60_000),
  WORLD_TICK_MAX_STEPS: z.coerce.number().int().positive().default(60)
  ```

  In tests using `testEnv`, set:

  ```ts
  WORLD_TICK_ENABLED: false,
  WORLD_TICK_INTERVAL_MS: 60_000,
  WORLD_TICK_MAX_STEPS: 60
  ```

- [ ] **步骤 2：给 app DI 增加 world runtime**

  In `apps/server/src/app.ts`, extend `di`:

  ```ts
  worldRuntime: {
    settleDue(now?: Date): Promise<{ settledSteps: number; skipped: boolean }>;
  };
  ```

  Create the runtime service in `buildApp()`:

  ```ts
  const worldRuntime = {
    settleDue: async (now = new Date()) => {
      const repo = new WorldRuntimeRepository(db);
      const npcRepo = new NpcRepository(db);
      const npcService = new NpcService(npcRepo);
      const runtime = new WorldRuntimeService({
        repo,
        ownerId: `server-${process.pid}`,
        maxStepsPerRun: config.WORLD_TICK_MAX_STEPS,
        settleNpcWorld: (tickAt) => npcService.settleNpcWorld(tickAt)
      });
      return runtime.settleDue(now);
    }
  };
  ```

  Decorate:

  ```ts
  app.decorate("di", { db, worldRuntime });
  ```

- [ ] **步骤 3：添加受 env 控制的后台 interval**

  In `apps/server/src/app.ts` after route registration setup:

  ```ts
  let worldTickTimer: NodeJS.Timeout | null = null;

  if (config.WORLD_TICK_ENABLED) {
    worldTickTimer = setInterval(() => {
      void worldRuntime.settleDue().catch((error) => {
        app.log.error({ err: error }, "world runtime settlement failed");
      });
    }, config.WORLD_TICK_INTERVAL_MS);
  }

  app.addHook("onClose", async () => {
    if (worldTickTimer) clearInterval(worldTickTimer);
  });
  ```

  Keep the existing DB close hook.

- [ ] **步骤 4：玩家读取状态时按需补结算**

  In `apps/server/src/modules/game/game.routes.ts`, extend `GameRouteDependencies`:

  ```ts
  settleWorldIfDue(): Promise<void>;
  ```

  Add the default dependency:

  ```ts
  settleWorldIfDue: async () => {
    await app.di.worldRuntime.settleDue(new Date());
  },
  ```

  Before `getState(account.id)` in `GET /game/state`, call:

  ```ts
  await deps.settleWorldIfDue();
  ```

  Test in `apps/server/src/modules/game/game.routes.test.ts`:

  ```ts
  it("settles the NPC world before returning game state", async () => {
    const settleCalls: string[] = [];
    const app = buildGameRouteTestApp({
      getCurrentAccount: async () => account,
      getState: async () => baseState,
      settleWorldIfDue: async () => {
        settleCalls.push("settled");
      }
    });

    await app.inject({ method: "GET", url: "/game/state" });

    expect(settleCalls).toEqual(["settled"]);
  });
  ```

- [ ] **步骤 5：管理员读取 NPC 时按需补结算**

  In `apps/server/src/modules/admin/admin.routes.ts`, `getNpcSnapshot()` and `settleNpcWorld()` should call `app.di.worldRuntime.settleDue()` instead of directly constructing `NpcService` for true world progression.

  Keep `POST /admin/npcs/settle` as an explicit “settle now” action, but route it through the runtime service so it respects lease and cursor.

- [ ] **步骤 6：运行路由测试**

  ```bash
  CI=true pnpm --filter @ai-mud/server test -- app.test.ts game.routes.test.ts admin.routes.test.ts world-runtime.service.test.ts
  CI=true pnpm --filter @ai-mud/server typecheck
  ```

- [ ] **步骤 7：提交**

  ```bash
  git add apps/server/src/config/env.ts apps/server/src/app.ts apps/server/src/app.test.ts apps/server/src/modules/game/game.routes.ts apps/server/src/modules/game/game.routes.test.ts apps/server/src/modules/admin/admin.routes.ts apps/server/src/modules/admin/admin.routes.test.ts
  git commit -m "Run NPC world settlement from the server runtime"
  ```

## 6. 任务 5：在后台 UI 展示运行时状态

目标：管理员能看到世界自动运行状态，不只是 NPC 当前状态。

### 文件

- Modify: `packages/shared/src/game.ts`
- Modify: `packages/shared/src/game.test.ts`
- Modify: `apps/server/src/modules/admin/admin.routes.ts`
- Modify: `apps/server/src/modules/admin/admin.routes.test.ts`
- Modify: `apps/web/src/features/admin/adminApi.ts`
- Modify: `apps/web/src/features/admin/NpcAdmin.tsx`
- Modify: `apps/web/src/features/admin/NpcAdmin.test.tsx`

### 步骤

- [ ] **步骤 1：扩展 shared DTO**

  Add to `packages/shared/src/game.ts`:

  ```ts
  export interface WorldRuntimeStatusDto {
    key: "npc_world";
    generatedAt: string;
    lastSettledAt: string | null;
    nextTickAt: string | null;
    leaseOwner: string | null;
    leaseUntil: string | null;
  }
  ```

  Add test:

  ```ts
  it("describes NPC world runtime status", () => {
    const status: WorldRuntimeStatusDto = {
      key: "npc_world",
      generatedAt: "2026-07-01T12:00:00.000Z",
      lastSettledAt: "2026-07-01T11:59:00.000Z",
      nextTickAt: "2026-07-01T12:00:00.000Z",
      leaseOwner: null,
      leaseUntil: null
    };

    expect(status.key).toBe("npc_world");
  });
  ```

- [ ] **步骤 2：添加后台状态 API**

  Add `GET /admin/world-runtime` returning `WorldRuntimeStatusDto`.

  Route test:

  ```ts
  it("returns world runtime status for admins", async () => {
    const app = buildAdminRouteTestApp({
      getCurrentAdmin: async () => ({
        id: "admin-1",
        email: "admin@example.com",
        role: "admin"
      }),
      getWorldRuntimeStatus: async () => ({
        key: "npc_world",
        generatedAt: "2026-07-01T12:00:00.000Z",
        lastSettledAt: "2026-07-01T11:59:00.000Z",
        nextTickAt: "2026-07-01T12:00:00.000Z",
        leaseOwner: null,
        leaseUntil: null
      })
    });

    const response = await app.inject({ method: "GET", url: "/admin/world-runtime" });

    expect(response.statusCode).toBe(200);
    expect(response.json().lastSettledAt).toBe("2026-07-01T11:59:00.000Z");
  });
  ```

- [ ] **步骤 3：在 `NpcAdmin` 渲染运行状态**

  Add `getWorldRuntimeStatus()` to `adminApi.ts`.

  In `NpcAdmin.tsx`, show:

  - `上次结算`
  - `下次 Tick`
  - `租约状态`

  Test:

  ```ts
  expect(screen.getByText("上次结算 2026-07-01 11:59")).toBeTruthy();
  expect(screen.getByText("下次 Tick 2026-07-01 12:00")).toBeTruthy();
  ```

- [ ] **步骤 4：运行 web 测试**

  ```bash
  CI=true pnpm --filter @ai-mud/shared test -- game.test.ts
  CI=true pnpm --filter @ai-mud/server test -- admin.routes.test.ts
  CI=true pnpm --filter @ai-mud/web test -- NpcAdmin.test.tsx
  CI=true pnpm --filter @ai-mud/web typecheck
  ```

- [ ] **步骤 5：提交**

  ```bash
  git add packages/shared/src/game.ts packages/shared/src/game.test.ts apps/server/src/modules/admin/admin.routes.ts apps/server/src/modules/admin/admin.routes.test.ts apps/web/src/features/admin/adminApi.ts apps/web/src/features/admin/NpcAdmin.tsx apps/web/src/features/admin/NpcAdmin.test.tsx
  git commit -m "Show NPC world runtime status in admin"
  ```

## 7. 任务 6：发布说明和验证

目标：用证据证明 v0.5.1 解决了自动运行、离线补结算和仿真只读问题。

### 文件

- Create: `docs/releases/v0.5.1.md`
- Modify: `apps/web/e2e/auth-smoke.spec.ts`

### 步骤

- [ ] **步骤 1：更新 E2E mock**

  In `apps/web/e2e/auth-smoke.spec.ts`, mock:

  ```ts
  await page.route("**/admin/world-runtime", async (route) =>
    route.fulfill({
      json: {
        key: "npc_world",
        generatedAt: "2026-07-01T12:00:00.000Z",
        lastSettledAt: "2026-07-01T11:59:00.000Z",
        nextTickAt: "2026-07-01T12:00:00.000Z",
        leaseOwner: null,
        leaseUntil: null
      }
    })
  );
  ```

  Add assertion:

  ```ts
  await expect(page.getByText("上次结算")).toBeVisible();
  ```

- [ ] **步骤 2：添加中文发布说明**

  Create `docs/releases/v0.5.1.md`:

  ```md
  # v0.5.1 世界自动运转

  ## 管理员可见内容

  - NPC 世界会按服务端现实时间自动推进。
  - 后台可以看到 NPC 世界运行状态、上次结算时间和下次 Tick 时间。
  - NPC 仿真报告改为只读预演，不再修改真实世界。

  ## 引擎边界

  - 新增世界运行时状态表，记录 NPC 世界结算游标和租约。
  - 新增自动结算服务，后台手动推进、玩家上线补结算和服务端定时器共用同一入口。
  - 离线补结算有最大步数限制，避免长时间离线后一次请求卡死。

  ## 本版本不包含

  - 新 NPC。
  - AI 对话。
  - AI 任务。
  - 资源刷新和平衡调参。
  - 多服强一致世界模拟。
  ```

- [ ] **步骤 3：完整验证**

  ```bash
  CI=true pnpm -r test
  CI=true pnpm -r typecheck
  CI=true pnpm -r build
  CI=true pnpm --filter @ai-mud/web e2e
  ```

- [ ] **步骤 4：最终自审清单**

  - [ ] `POST /admin/npcs/simulate` does not mutate real NPC state.
  - [ ] `GET /game/state` triggers bounded catch-up before returning player state.
  - [ ] `GET /admin/npcs` sees a current NPC world snapshot.
  - [ ] Background interval is disabled in tests.
  - [ ] Runtime lease prevents duplicate settlement under concurrent calls.
  - [ ] Long offline periods are bounded by `WORLD_TICK_MAX_STEPS`.
  - [ ] No AI asset authority was introduced.

- [ ] **步骤 5：提交**

  ```bash
  git add apps/web/e2e/auth-smoke.spec.ts docs/releases/v0.5.1.md
  git commit -m "Document and verify world autopilot"
  ```

## 8. 执行顺序

推荐执行顺序：

1. Task 1 first. It fixes the current v0.5.0 simulation mutation bug.
2. Task 2 and Task 3 can be implemented by separate agents after Task 1 lands.
3. Task 4 depends on Task 3.
4. Task 5 depends on Task 4.
5. Task 6 is final release verification only.

Do not start AI/NPC personality work in this version.

Do not add new economy balancing constants in this version unless a test proves the runtime cannot progress without them.
