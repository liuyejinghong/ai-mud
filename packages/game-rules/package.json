{
  "name": "@ai-mud/game-rules",
  "private": true,
  "type": "module",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "import": "./dist/index.js"
    }
  },
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "typecheck": "pnpm --filter @ai-mud/shared build && pnpm --filter @ai-mud/content build && tsc -p tsconfig.json --noEmit",
    "test": "pnpm --filter @ai-mud/shared build && pnpm --filter @ai-mud/content build && vitest run --passWithNoTests",
    "lint": "pnpm --filter @ai-mud/shared build && pnpm --filter @ai-mud/content build && tsc -p tsconfig.json --noEmit"
  },
  "dependencies": {
    "@ai-mud/content": "workspace:*",
    "@ai-mud/shared": "workspace:*"
  },
  "devDependencies": {
    "typescript": "^5.5.0",
    "vitest": "^2.0.0"
  }
}
